let cards = [7, 3, 9]

// Create a for loop that logs out all the cards in the array
for (cardItem = 0; cardItem < cards.length; cardItem += 1) {
 console.log(cards[cardItem])
}
// Use cards.length to specify how long the loop should run
